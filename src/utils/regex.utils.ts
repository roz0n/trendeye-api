// Match all chars between parens including parens
export const parensRegex = /\(([^)]+)\)/g;
// Match all numertical chars
export const numsRegex = /[0-9]+/g;
// Math all possible new line chars
export const newLineRegex = /(?:\r\n|\r|\n)/g;