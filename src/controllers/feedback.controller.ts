const db = require("../db");

export default class FeedbackController {
  async savePositiveFeedback() {
    try {
    } catch (error) {}
  }

  async saveNegativeFeedback() {
    try {
    } catch (error) {}
  }
}
